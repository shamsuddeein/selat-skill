{
  "schema": "selat-skill/v1",
  "name": "sales-prospecting",
  "description": "Build targeted B2B prospect lists with verified contact information, fully MPP-via the SELAT Router. Searches companies (Fiber), finds decision-makers (Fiber), pulls all domain emails (Hunter), finds a specific contact's email (Hunter email finder), verifies deliverability (Hunter email verifier), and enriches with company data (Abstract Company Enrichment).",
  "maxAmount": "1.00",
  "params": {
    "companyQuery": {
      "required": false,
      "default": "B2B SaaS startups in San Francisco with 50-200 employees Series A or B funded",
      "description": "Free-form natural-language description of the target companies (ICP) for the Fiber company search."
    },
    "jobTitles": {
      "required": false,
      "default": "CTO,VP Engineering,Head of Engineering",
      "description": "Comma-separated decision-maker job titles for the Fiber people search."
    },
    "companyNames": {
      "required": false,
      "default": "Stripe,Figma,Notion",
      "description": "Comma-separated company names to scope the Fiber people search."
    },
    "locations": {
      "required": false,
      "default": "San Francisco",
      "description": "Comma-separated locations to scope the Fiber people search."
    },
    "domain": {
      "required": true,
      "default": "stripe.com",
      "description": "Company domain used for Hunter domain-search, the Hunter email-finder lookup, and the Abstract Company Enrichment lookup."
    },
    "firstName": {
      "required": false,
      "default": "Sarah",
      "description": "First name of the specific contact whose email the Hunter email finder should find."
    },
    "lastName": {
      "required": false,
      "default": "Chen",
      "description": "Last name of the specific contact whose email the Hunter email finder should find."
    },
    "email": {
      "required": false,
      "default": "sarah@stripe.com",
      "description": "Email address to verify for deliverability via the Hunter email verifier before adding to a sequence."
    }
  },
  "steps": [
    {
      "label": "all emails for domain — Hunter domain search (MPP on Tempo)",
      "rail": "MPP on Tempo",
      "method": "POST",
      "url": "https://hunter.mpp.paywithlocus.com/hunter/domain-search",
      "body": {
        "domain": "${domain}"
      },
      "maxAmount": "1.00"
    },
    {
      "label": "find specific contact email — Hunter email finder (MPP on Tempo)",
      "rail": "MPP on Tempo",
      "method": "POST",
      "url": "https://hunter.mpp.paywithlocus.com/hunter/email-finder",
      "body": {
        "domain": "${domain}",
        "first_name": "${firstName}",
        "last_name": "${lastName}"
      },
      "maxAmount": "0.15"
    },
    {
      "label": "verify email deliverability — Hunter email verifier (MPP on Tempo)",
      "rail": "MPP on Tempo",
      "method": "POST",
      "url": "https://hunter.mpp.paywithlocus.com/hunter/email-verifier",
      "body": {
        "email": "${email}"
      },
      "maxAmount": "0.10"
    },
    {
      "label": "enrich with company data — Abstract Company Enrichment lookup (MPP on Tempo)",
      "rail": "MPP on Tempo",
      "method": "POST",
      "url": "https://abstract-company-enrichment.mpp.paywithlocus.com/abstract-company-enrichment/lookup",
      "body": {
        "domain": "${domain}"
      },
      "maxAmount": "0.10"
    }
  ]
}
