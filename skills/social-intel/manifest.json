{
  "schema": "selat-skill/v1",
  "name": "social-intel",
  "description": "Grounded web-context intelligence on any topic, brand, or account — cross-checks two independent web searches (Exa neural + Tavily advanced) for corroborated results with citations, paid per call over x402, via the SELAT Router (selat-pay, USDC via Circle Gateway).",
  "maxAmount": "0.10",
  "params": {
    "topic": {
      "required": true,
      "default": "agent payments",
      "description": "Keyword/topic to search the web for (used as ${topic} in both search queries)."
    }
  },
  "steps": [
    {
      "label": "web context — Exa search (MPP on Tempo)",
      "rail": "MPP on Tempo",
      "method": "POST",
      "url": "https://api.exa.ai/search",
      "body": {
        "query": "${topic}",
        "numResults": 10,
        "contents": {
          "text": {
            "maxCharacters": 4000
          }
        }
      },
      "maxAmount": "0.05"
    },
    {
      "label": "web corroboration — Tavily search (x402 on Base)",
      "rail": "x402 on Base",
      "method": "POST",
      "url": "https://x402.tavily.com/search",
      "body": {
        "query": "${topic}",
        "search_depth": "advanced",
        "max_results": 10
      },
      "maxAmount": "0.05"
    }
  ]
}
